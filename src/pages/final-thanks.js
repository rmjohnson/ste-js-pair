import React from 'react';
import { Button } from '../components/button';

export const FinalThanks = () => {
	const handleClick = () => {
		// TODO!
	};

	return (
		<div>
			<h2>Thank you!</h2>
			<Button label="Book your next stay" onClick={handleClick} />
		</div>
	);
};
